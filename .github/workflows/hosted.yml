# This workflow will do a clean install of node dependencies, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: Mac mini CI

on:
  push:
    branches: [ github-actions ]
     
jobs:
  build:

    runs-on: [self-hosted, macOS]
    env:
      ELEVENTY_ENV: dev

    steps:
    - uses: actions/checkout@v2
    - name: Build site 
      run: |
        yarn install
        yarn run build
  publish:
    runs-on: [self-hosted, macOS]
    name: Publish site to Staging
    needs: [build]
    env:
      TEST: dan 
      USER: ${{ secrets.BYG_USER }}
      SERVER: ${{ secrets.BYG_IP }}
      X: ${{ secrets.ABCD }} 
    steps:
      - name: Upload
        run: |
          echo "$TEST" "$USER" "$SERVER" 
          echo "$USER" > tmp.txt
          ls -la
  # publish:  
  #  - name: Publish
  #    env:
  #      USER: ${{ secrets.BYG_USER }}
  #      SERVER: ${{ secrets.BYG_IP }}  
  #    run: | 
  #      ssh "$USER"@"$SERVER"
    # - run: tar -czvf site.tar.gz dist
    # - name: Upload build
    #   uses: actions/upload-artifact@v1
    #   with:
    #     name: build
    #     path:  site.tar.gz
  # deploy_staging:
  #   needs: [build]
  #   runs-on: [self-hosted, macOS]
  #   steps:
  #     - name: Get built site
  #       uses: actions/download-artifact@v1
  #       with:
  #         name: build
  #     - name: Publish
  #       env:
  #         USER: ${{ secrets.bgyUser }}
  #         SERVER: ${{ secrets.bgyIP }}  
  #       run: | 
  #         scp -r build/site.tar.gz $SSH_USER@$SSH_HOST:~/backgroundyellow.net
  #         ssh $SSH_USER@$SSH_HOST tar -xzvf backgroundyellow.net/site.tar.gz -C backgroundyellow.net/v2l/test

---
layout: layouts/default.liquid
test: lean management 2
---

{% comment %} 
courses/{{ item.data.course-title | slug }}/{{ item.data.unit-title | slug }}/{{ item.data.session-title | slug }}/{{ item.data.title | slug }}/index.html
{% endcomment %}

<ul>
  {%- for item in collections.lm-2-u1-s1 -%}
    <li>
      <a href="{{ item.course-title }}">{{ item.data.title }}</a>
    </li>
  {%- endfor -%}
</ul>

<hr>

<ul>
  {%- for item in collections["lm-2-u1-s2"] -%}
    <li>
      <a href="{{ item.course-title }}">{{ item.data.title }}</a>
    </li>
  {%- endfor -%}
</ul>
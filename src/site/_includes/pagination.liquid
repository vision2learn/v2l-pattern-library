<nav>
  <ol aria-label="Pages in this session" role="list" class="c-pagination">
    {%- if pagination.previousPageHref -%}
      <li class="c-pagination__prev" role="listitem">
        <a href="{{ pagination.previousPageHref }}">
          <i class="visually-hidden">Previous Page</i>
          {% include 'icon', icon: 'chevron-left' %}
        </a>  
      </li>
    {%- endif -%}

    {% comment %} TODO: Re-order visually with CSS {% endcomment %}
    {%- if pagination.nextPageHref -%}
      <li class="c-pagination__next" role="listitem">
        <a href="{{ pagination.nextPageHref }}">
          <i class="visually-hidden">Next Page</i>
          {% include 'icon', icon: 'chevron-right' %}
        </a>
      </li>
    {%- endif -%}

    {%- for href in pagination.hrefs -%}
      {% comment %} TODO: Review component with VoiceOver {% endcomment %}
      <li role="listitem">
        <a href="{{ href }}" data-v2l-pagetitle="{{ page-titles[forloop.index0] }}" {% if page.url == href %}aria-current="page"{% endif %}>{{ forloop.index }}</a>
      </li>
    {%- endfor -%}
  </ol>
</nav>

{%- from "components/all.njk" import accordion as accordion %}

<div class="toggle-group">
{# {%- for reveal in section.subInc.content %} #}
  {{ accordion(section.subInc.content, site.env) }}
{# {%- endfor %} #}
</div>

{# title=reveal.title, copy=reveal.copy, img=reveal.img #}
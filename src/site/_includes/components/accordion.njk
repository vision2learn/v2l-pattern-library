{% import "components/all.njk" as components %}

<div class="toggle-group">
{% for reveal in section.subInc.content %}
  {{ components.accordion(reveal, pagedata.env) }}
{% endfor %}
</div>

{# title=reveal.title, copy=reveal.copy, img=reveal.img #}
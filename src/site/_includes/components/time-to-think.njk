{% import "components/all.njk" as components %}

{{ components.media2({
    title: "Time to think",
    copy: section.include.copy,
    layout: "media-half",
    media: {
      ref: "think"
    }
  }, module.data.buildenv) }}
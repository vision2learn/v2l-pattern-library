{%- set unique -%}
  {{ '' | uniqueID }}
{%- endset -%}
<div class="l-media l-media--half l-media--max">
  <h2 class="l-media__title" id="ttt_{{ unique | trim }}">
    Time to Think {{ section.include.layout }}
  </h2>
  <div class="l-media__body" id="ttt_desc_{{ unique | trim }}">
    {{ section.include.copy | markdownify | safe }}
  </div>
  {%- if site.env !== 'dotnet'%}
    <form class="l-media__img l-think" id="{{ 'formJournal' | uniqueID }}">
      <textarea class="js-think" rows="15" aria-labelledby="ttt_{{ unique | trim }}" aria-describedby="ttt_desc_{{ unique | trim }}"></textarea>
      <button>Save</button>
    </form>
  {%- else %}
    @Html.Partial("_JournalAddEntry", new JournalData((int)@ViewBag.CurrentUnit,
      (int)@ViewBag.CurrentSession, (int)@ViewBag.CurrentPage, @ViewBag.CurrentSessionTitle,
      "{{ section.include.copy | striptags(false)}}",
      formClassAttribute: "l-media__img l-think", formIdAttribute: "{{ 'formJournal' | uniqueID }}"))
  {%- endif %}
</div>